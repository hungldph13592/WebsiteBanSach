@model IEnumerable<WebBanSach.Models.NhaXuatBan>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenXuatBan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrangThai)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenXuatBan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrangThai)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_NXB">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_NXB">Details</a> |
@*                <a asp-action="Delete" asp-route-id="@item.ID_NXB">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
